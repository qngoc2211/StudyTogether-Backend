# Server - Render s\u1ebd t\u1ef1 \u0111\u1ed9ng g\u00e1n PORT
server.port=${PORT:8080}

# Database PostgreSQL Render
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME:studytogether_422m_user}
spring.datasource.password=${DATABASE_PASSWORD:vrF2A2Io0yASOYrySf3hnf1KbKBwjiRG}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA - Hibernate t\u1ef1 \u0111\u1ed9ng ch\u1ecdn dialect cho PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# JWT
jwt.secret=${JWT_SECRET:StudyTogetherSecretKey2024VerySecureKey}
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql=INFO